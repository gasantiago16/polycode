---
name: deep-research
description: >
  Cited research report. Prefer /deep-research which fans out researcher
  children. Use web_search then web_fetch primaries. Do not answer from memory.
---

# Deep research

1. Search (web_search). Cluster into at most 3 angles.
2. Fetch primaries (web_fetch). Blogs that only repeat vendor adjectives do not count.
3. Write a cited report to `docs/research/<slug>.md` with: question, findings, sources (URL), holes, what we would not claim.
4. Fail closed: if you cannot fetch a primary, verdict is unknown — not "holds".

Query: $ARGUMENTS
